# Copy this file to .env and fill in your Supabase project's values
# (Supabase dashboard → Project Settings → API).
# The app works fine without these — progress just stays on this device only.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
